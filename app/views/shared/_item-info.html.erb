<div class="font-weight-bold mr-2">
  <%= link_to user.name, user_path(user) %>
</div>
<small>
  <time class="text-black-50" datetime="<%= created_at %>">
    <%= created_at.strftime("%Y/%m/%d") %>
  </time>
</small>