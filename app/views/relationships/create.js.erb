{
  $(`#follow-<%= @user.id %>`).html(`<%= j(render "relationships/btn", user: @user) %>`);

  <%= render "relationships/js/update-count", user: @user %>
  <%= render "relationships/js/update-count", user: current_user %>
}