{
  $(`#follow-<%= @user.id %>`).html(`<%= render "relationships/btn", user: @user %>`);

  <%= render "relationships/update-count", user: @user %>
  <%= render "relationships/update-count", user: current_user %>
}