{
  const user_id = <%= @user.id %>;
  const current_user_id = <%= current_user.id %>;

  $(`#follow-${user_id}`).html(`<%= render "relationships/btn", user: @user %>`);
  $(`.user-followings-count[data-user-id="${user_id}"]`).html(`<%= render "relationships/followings-count", user: @user %>`);
  $(`.user-followers-count[data-user-id="${user_id}"]`).html(`<%= render "relationships/followers-count", user: @user %>`);

  $(`.user-followings-count[data-user-id="${current_user_id}"]`).html(`<%= render "relationships/followings-count", user: @user %>`);
  $(`.user-followers-count[data-user-id="${current_user_id}"]`).html(`<%= render "relationships/followers-count", user: @user %>`);

  console.log("destroy");
}